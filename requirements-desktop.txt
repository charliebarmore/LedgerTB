# Full application + test/build tooling used by scripts/build_release.sh.
-r requirements-dev.txt

# Desktop app: native window (Tier 1) + standalone .app build (Tier 2)
pywebview>=6.2.1
pyinstaller>=6.21.0
