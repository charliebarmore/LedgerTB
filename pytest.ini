[pytest]
markers =
    performance: representative high-volume regression baselines using temporary data
    real_vault: exercises utils.secure_store itself (stubs keyring directly), bypassing the autouse fake vault
